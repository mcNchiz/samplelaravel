{
  "compilerOptions": {
    "target": "esnext",                                   /* Specify a set of bundled library declaration files that describe the target runtime environment. */
    "jsx": "react",
    "module": "esnext",                                   /* Specify the root folder within your source files. */
    "moduleResolution": "node",                                /* Specify multiple folders that act like './node_modules/@types'. */
    "types": ["vite/client"],             /* Allow 'import x from y' when a module doesn't have a default export. */
    "esModuleInterop": true,                        /* Disable resolving symlinks to their realpath. This correlates to the same flag in node. */
    "forceConsistentCasingInFileNames": true,            /* Ensure that casing is correct in imports. */
    "strict": true,                     /* Skip type checking .d.ts files that are included with TypeScript. */
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "paths": {
      "@components/*": ["./resources/ts/Components/*"],
      "@layouts/*": ["./resources/ts/Layouts/*"],
      "@utilities/*": ["./resources/ts/Utilities/*"],
      "@constants/*": ["./resources/ts/Constants/*"]
    }                                 /* Skip type checking all .d.ts files. */
  }
}